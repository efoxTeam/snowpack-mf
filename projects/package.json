{
  "name": "projects",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "lerna run --scope esm-* --parallel dev",
    "build": "lerna run --scope esm-* --parallel build",
    "start": "lerna run --scope esm-* --parallel start"
  },
  "devDependencies": {},
  "dependencies": {}
}