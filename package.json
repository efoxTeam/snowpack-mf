{
  "name": "esm-mf",
  "version": "1.0.1-0",
  "main": "index.js",
  "private": true,
  "repository": "https://github.com/efoxTeam/snowpack-mf.git",
  "author": "xuhongbin <xuhongbin@yy.com>",
  "license": "MIT",
  "devDependencies": {
    "@efox/eslint-config-react-prittier-ts": "^1.2.3"
  },
  "scripts": {
    "dev": "pnpm run --filter={app,host} --parallel dev",
    "build": "pnpm run --filter={app,host} --parallel build",
    "start": "pnpm run --filter={app,host} --parallel start"
  }
}
